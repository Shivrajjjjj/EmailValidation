<style>
    body { background-color: #ffffff; }
    .app-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; max-width: 800px; margin: 40px auto; }
    .app-card { border: 1px solid #dadce0; border-radius: 8px; padding: 20px; text-align: center; transition: 0.2s; cursor: pointer; text-decoration: none; display: block; color: inherit; }
    .app-card:hover { background-color: #f8f9fa; box-shadow: 0 1px 2px 0 rgba(60,64,67,0.30), 0 1px 3px 1px rgba(60,64,67,0.15); border-color: transparent; }
    .app-icon { font-size: 48px; margin-bottom: 15px; }
</style>

<div class="container mt-5 text-center">
    <h1 class="display-5 fw-normal mb-4" style="color: #202124;">Welcome to Workspace</h1>
    <p class="text-muted mb-5">Select an application to begin</p>

    <div class="app-grid">
        <a href="/Contact/Index" class="app-card">
            <span class="material-icons app-icon text-primary">mail</span>
            <h5 class="mb-2">Compose</h5>
            <p class="text-muted small">Submit new contact details</p>
        </a>

        <a href="/Contact/Dashboard" class="app-card">
            <span class="material-icons app-icon text-success">dashboard</span>
            <h5 class="mb-2">Inbox</h5>
            <p class="text-muted small">View stats & submissions</p>
        </a>

        <a href="/Contact/BlockedEmail" class="app-card">
            <span class="material-icons app-icon text-danger">gpp_bad</span>
            <h5 class="mb-2">Spam Filter</h5>
            <p class="text-muted small">Review blocked attempts</p>
        </a>
    </div>
</div>